class Git {
    static branchRegex = /\*\ (.+?)\n/;

    static branch() {
        // let output = exec(`git branch`)
        // ouput.exec(branchRegex)
    }

    static status() {
        // let output = exec(`git status`)
    }
}

module.exports = Git;